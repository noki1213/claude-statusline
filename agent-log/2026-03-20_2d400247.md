# セッションログ

- 日付：2026-03-20
- セッションID：2d400247-47d7-40ea-a60d-b62114eb956d
- モデル：claude-sonnet-4-6

---

## 概要

Claude Code v2.1.80 で公式に追加された `rate_limits` フィールドを使い、
5時間・7日間のレートリミット表示を非公式の方法から公式の方法に切り替えた。

---

## 背景

### 変更前の方法（非公式・規約グレー）

`fetch_usage` 関数が以下の処理をしていた：

1. キーチェーンから Claude Code の認証トークンを取り出す
2. Haiku モデルに curl で最小リクエストを送る
3. レスポンスのヘッダー（`anthropic-ratelimit-unified-5h-utilization` など）から使用率を読み取る
4. `/tmp/claude-usage-cache.json` にキャッシュして 360 秒間使い回す

問題点：
- Anthropic API に直接リクエストを送るため、API コストが少しかかる
- 認証トークンをスクリプトが直接扱う
- 非公式のヘッダーを利用しており、規約違反の可能性があった

### 変更後の方法（公式）

v2.1.80 のリリースで、Claude Code がステータスラインスクリプトに渡す JSON に
`rate_limits` フィールドが公式に追加された。

渡ってくる JSON の構造（実測値）：
```json
{
  "rate_limits": {
    "five_hour": {
      "used_percentage": 6,
      "resets_at": 1774000800
    },
    "seven_day": {
      "used_percentage": 81,
      "resets_at": 1774317600
    }
  }
}
```

注意点：ブログ記事（nyosegawa.com）では `resets_at` が ISO 8601 形式の文字列
（例：`"2026-03-20T15:00:00Z"`）と書かれていたが、実際にはエポック秒の整数で渡ってくる。

---

## 作業内容

### ブランチ：`official-rate-limits`

#### コミット 1：`feat: rate_limits の取得を公式フィールドに切り替える`

削除した処理（90行ぶん）：
- `fetch_usage` 関数（curl でAPIを叩く処理）
- `load_usage` 関数（キャッシュから読み込む処理）
- キャッシュファイル `/tmp/claude-usage-cache.json` の読み書き処理
- `to_pct` 関数（0.0〜1.0 の小数を % に変換していた）

追加した処理（23行ぶん）：
- jq で `rate_limits.five_hour.used_percentage` / `resets_at` を読み取る
- jq で `rate_limits.seven_day.used_percentage` / `resets_at` を読み取る
- `iso_to_epoch` 関数（ISO 8601 → エポック秒の変換）※後で削除

#### コミット 2：`fix: resets_at がエポック秒として渡ってくることに対応する`

実際に渡ってくる JSON を確認したところ、`resets_at` が ISO 8601 ではなく
整数のエポック秒だったため、不要になった `iso_to_epoch` 関数を削除した。

### mainへのマージ

`fast-forward` マージで main に取り込んだ。

---

## デバッグ方法

実際に渡ってくる JSON を確認するため、スクリプトに一時的に以下を追加した：

```bash
echo "$input" > /tmp/statusline-debug.json
```

確認後に削除した。
